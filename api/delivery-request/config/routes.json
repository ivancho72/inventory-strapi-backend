{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-requests",
      "handler": "delivery-request.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-requests/count",
      "handler": "delivery-request.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-requests/:id",
      "handler": "delivery-request.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/delivery-requests",
      "handler": "delivery-request.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-requests/:id",
      "handler": "delivery-request.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-requests/:id",
      "handler": "delivery-request.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
